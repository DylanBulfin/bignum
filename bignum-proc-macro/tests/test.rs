#[cfg(test)]
mod tests {
    #[test]
    fn testing_bare() {
        //create_efficient_base_bare!(123);
        //create_efficient_base_bare!(34524);
        //
        //for (i, power) in __BASE_123_POWERS.iter().enumerate() {
        //    assert_eq!(123u64.pow(i as u32), *power);
        //}
        //
        //for (i, power) in __BASE_34524_POWERS.iter().enumerate() {
        //    assert_eq!(34524u64.pow(i as u32), *power);
        //}
        //for (i, power) in __BASE_34524_U128_POWERS.iter().enumerate() {
        //    assert_eq!(34524u128.pow(i as u32), *power);
        //}
    }
}
